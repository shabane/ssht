# ssht TODO & Roadmap 🚀

لیست اقدامات و پیشنهادات توسعه برای پروژه **ssht**، دسته‌بندی‌شده بر اساس اولویت و حوزه فنی.

---

## 🟢 فاز ۱: بردهای سریع و قابلیت‌های کلیدی (Quick Wins & High Value)

- [ ] **جستجو در IP و User (علاوه بر نام هاست)**
  - *توضیح:* در حال حاضر تابع `SearchHostname` فقط روی نام مستعار (Host Alias) فیلتر می‌کند. کاربر باید بتواند با وارد کردن IP یا نام کاربری سرور مورد نظرش را پیدا کند.
  - *محل اعمال:* `sshUtils/sshUtils.go` و `component/searchBox.go`

- [ ] **مدیریت خطای تمیز به جای `panic`**
  - *توضیح:* اگر فایل `~/.ssh/config` وجود نداشته باشد یا دسترسی خواندن نداشته باشد، برنامه کرش می‌کند (`panic`). برنامه باید پیام خطای واضح به کاربر نشان داده و با کد خطای تمیز خارج شود.
  - *محل اعمال:* `sshUtils/sshUtils.go`

- [ ] **بررسی نصب بودن `tmux`**
  - *توضیح:* قبل از اجرای دستورات مربوط به tmux، بررسی شود که باینری `tmux` روی سیستم نصب است (`exec.LookPath("tmux")`). در صورت عدم وجود، پیام راهنما برای نصب آن نمایش داده شود.
  - *محل اعمال:* `tmuxUtils/connectAll.go`

- [ ] **پشتیبانی از پرچم‌های ورودی خط فرمان (CLI Flags)**
  - [ ] `-c, --config <path>`: مشخص کردن مسیر دلخواه برای فایل ssh config (پیش‌فرض: `~/.ssh/config`)
  - [ ] `--no-ping`: اجرای برنامه بدون پینگر و بررسی سلامت شبکه (مناسب برای محیط‌های ایزوله یا سرعت بالاتر)
  - [ ] `-v, --version`: نمایش نسخه برنامه
  - [ ] `-h, --help`: راهنمای دستورات خط فرمان

---

## 🟡 فاز ۲: بهبود تجربه کاربری و رابط کاربری (UI & UX Enhancements)

- [ ] **امکان انتخاب موردی با کلید `Space` (Multi-Selection)**
  - *توضیح:* کاربر بتواند با زدن کلید `Space` روی هر ردیف، یک تیک (`[x]`) کنار هاست بزند و با زدن شرتکات‌ها (`Ctrl+A`, `Ctrl+O`, ...) فقط هاست‌های تیک‌خورده باز شوند؛ در صورتی که هیچ هاستی تیک نخورده بود، طبق روال فعلی تمام هاست‌های فیلتر شده باز شوند.
  - *محل اعمال:* `component/listBox.go` و `tmuxUtils/connectAll.go`

- [ ] **نوار وضعیت (Status Bar / Keybindings Footer)**
  - *توضیح:* قرار دادن یک نوار ساده و کم‌ارتفاع در پایین صفحه با خلاصه‌ای از کلیدهای پرکاربرد (`Enter: Connect | Space: Select | Ctrl+O: Sync | ?: Guide`) تا کاربر نیازی به حفظ کردن همه کلیدها نداشته باشد.

- [ ] **مدیریت هوشمندانه‌تر سرورهای پشت Bastion / ProxyJump**
  - *توضیح:* سرورهایی که با `ProxyJump` کانفیگ شده‌اند و IP محلی دارند، توسط پینگر فعلی قرمز نشان داده می‌شوند. اضافه کردن نشانگر خاص (مثلاً علامت `[proxy]` یا زرد رنگ) برای هاست‌هایی که پروکسی دارند تا کاربر بداند چرا پینگ مستقیم نمی‌شوند.
  - *محل اعمال:* `pinger.go` و `component/listBox.go`

- [ ] **مرتب‌سازی هوشمند (Sorting)**
  - *توضیح:* امکان قرار گرفتن هاست‌های در دسترس (سبز) در بالای لیست یا مرتب‌سازی بر اساس آخرین استفاده‌ها (MRU - Most Recently Used).

---

## 🔵 فاز ۳: مهندسی کد و معماری (Code Quality & Architecture)

- [ ] **اصلاح تایپوی نام پکیج `tvewUtils`**
  - *توضیح:* تغییر نام پکیج به `tviewUtils` جهت رعایت استانداردهای نام‌گذاری.

- [ ] **کاهش وابستگی به وضعیت عمومی (Refactoring Global State)**
  - *توضیح:* انتقال متغیرهای گلوبال پکیج‌ها (`sshUtils.AllHosts`, `tmuxUtils.SelectedHosts`) به یک ساختار حالت مرکزی (مانند `type AppState struct`) برای افزایش خوانایی و جلوگیری از race conditions.

- [ ] **نوشتن تست‌های واحد (Unit Tests)**
  - [ ] تست برای `sshUtils.SearchHostname`
  - [ ] تست برای `sshUtils.GetAllHosts` با فایل‌های نمونه ssh config
  - [ ] تست برای فرمت‌بندی ردیف‌ها (`FormatHost`)

- [ ] **پشتیبانی کامل از دایرکتیو `Include` در SSH Config**
  - *توضیح:* اطمینان از اینکه فایل‌های چندبخشی کانفیگ (مثل `Include ~/.ssh/conf.d/*`) به درستی لود و ادغام می‌شوند.

---

## 🟣 فاز ۴: اتوماسیون، انتشار و مستندات (DevOps & CI/CD)

- [ ] **راه‌اندازی GitHub Actions Workflow**
  - *توضیح:* اجرای خودکار `go test`، `go vet` و `golangci-lint` روی پول ریکوئست‌ها.

- [ ] **یکپارچه‌سازی با GoReleaser**
  - *توضیح:* کامپایل خودکار و بیلد Assetها برای Linux (amd64, arm64) و macOS (Intel, Apple Silicon) هنگام زدن تگ نسخه جدید.

- [ ] **بهبود اسناد و متن README**
  - [ ] اصلاح تایپوها (`symontiniusly` -> `simultaneously`, `guaranty` -> `guarantee`, `syncronize` -> `synchronize`)
  - [ ] افزودن راهنمای ساخت از سورس و امکانات جدید به README
